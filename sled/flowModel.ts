export { flowModel } from '../target/generated/sledFlowModel';
// This module just proxies `flowModel` from `target` directory, so it'd be easier to consume from other modules inside the `sled` directory
